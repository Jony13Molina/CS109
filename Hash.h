#ifndef Hash_h
#define Hash_h
#include <iostream>
#include <string>
#include <stdlib.h>
#include <assert.h>
#include <stdio.h>
#include <cstddef>


unsigned int P_Hashing(char* inId);
int hash(char* id);
unsigned int shift(unsigned int id, int is_shift);

#endif
